import { When, Then, Given } from "cypress-cucumber-preprocessor/steps"

// Given("I click on the search box", () => {
//   cy.get("#ons-quick-picker").click({ force: true })
// })

// When("I click the date search {string} option on the list", (dateOption) => {
//   cy.get("cy.get('[data-value=" + dateOption + "]')").click({ force: true })
// })

// Then("the {string} to sign in is {string} on the map", (message, displayed) => {
//   let is_element_exist = "not.exist"
//   displayed == "displayed" ? (is_element_exist = "exist") : (is_element_exist = "not.exist")
//   cy.get(".ons-feature-sign-in > :nth-child(1) > .col-10 > p")
//     .contains(message)
//     .should(is_element_exist, { force: true })
// })
