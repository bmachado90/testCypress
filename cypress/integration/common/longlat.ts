// import { When } from "cypress-cucumber-preprocessor/steps/index"

// function getLatitude(latitude: string): Cypress.Chainable<string> {
//   return cy.fixture("lonlat.json").its(latitude).its("latitude")
// }

// function getLongitude(latitude: string): Cypress.Chainable<string> {
//   return cy.fixture("lonlat.json").its(latitude).its("latitude")
// }

// When("the map zooms in to {string}", (address) => {
//   //convert location to lon&latitude
// })
